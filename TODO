This is a list of features that can be implemented

Keep track of the last entry number to ensure continuity upon starting the program again
Keywords that the user can enter to load the file into memory as a linked list (load)
Any improvements you can make
